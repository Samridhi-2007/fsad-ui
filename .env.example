## If you deploy frontend separately (no CRA proxy), set backend URL here:
# REACT_APP_API_URL=http://localhost:8082

## Optional: override if your Spring Boot auth endpoints are different
# REACT_APP_AUTH_LOGIN_PATH=/auth/login
# REACT_APP_AUTH_REGISTER_PATH=/auth/register
